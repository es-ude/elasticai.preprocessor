name: Release

on:
  release:
    types: [published]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup devenv
        uses: ./.github/actions/setup-devenv

      - name: Build package
        run: devenv tasks package:build

      - name: Upload release artifacts
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*